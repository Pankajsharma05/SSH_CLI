[package]
name = "ssh_cli"
version = "1.1.0"
edition = "2021"
authors = ["Pankaj Sharma"]
description = "SSH_CLI — dual-pane SSH file manager with remote and local terminals, built for cluster work"
readme = "../README.md"
license = "MIT"
keywords = ["ssh", "scp", "hpc", "slurm", "terminal"]
categories = ["command-line-utilities", "filesystem"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
dirs = "5"
portable-pty = "0.8"

# The Windows build speaks SSH in-process (see src/mux.rs), because
# Windows OpenSSH cannot multiplex a connection. libssh2 uses the native
# WinCNG crypto there, so this pulls in no OpenSSL.
[target.'cfg(windows)'.dependencies]
ssh2 = "0.9"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
